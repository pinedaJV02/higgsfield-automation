# Dependencies and bundled browser (regenerated by setup.bat)
node_modules/

# Generated images
output/

# Build artifacts
*.zip

# OS junk
Thumbs.db
.DS_Store

# Local Claude Code settings
.claude/settings.local.json

# NOTE: profile/ (the logged-in browser session) is intentionally NOT ignored,
# because the user chose to include the saved login in the transferable package.
# If you ever publish this code, add `profile/` here and never commit it.
